let members = ['Anik', 'Sayid', 'Rubel','Humayon'];
// using while loop
let i = 0;
while(i < members.length){
  let element = members[i];
  // console.log(element);
  i++;
}

// using for loop
for(let x =0; x <members.length; x++){
  let element = members[x];
  console.log(x,element);
}